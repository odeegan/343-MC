
select number of players
init the game
redraw the board
start players turn
prompt player to roll
check square
perform behavior

WHAT IS DRAWABLE?
board
player
score board



public class RollDiceMenu extends JPanel {
		
		public RollDiceMenu() {
			setPreferredSize(new Dimension(450,200));
			// make the background transparent
			setOpaque(false);

			JButton rollDiceButton = new JButton("Roll the Dice");
			rollDiceButton.addActionListener(
					new ActionListener() {
						public void actionPerformed(ActionEvent event) {
							GamePane.getInstance().getMessagePanel().removeAll();
						}
					});
			add(rollDiceButton);
		}
	}